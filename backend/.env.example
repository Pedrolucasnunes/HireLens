OPENAI_API_KEY=

# Memória vetorial de análises (opcional — sem estas variáveis o agente funciona
# normalmente, apenas sem o contexto de análises anteriores). Supabase usado só
# como Postgres gerenciado com pgvector; rodar supabase/migrations/ antes.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Origens extras para CORS em dev (separadas por vírgula). Padrão: só a landing publicada.
# CORS_ORIGENS=https://hire-lens-rust.vercel.app,http://localhost:3000
